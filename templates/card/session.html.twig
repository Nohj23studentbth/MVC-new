{% extends "base.html.twig" %}

{% block title %}Session{% endblock %}

{% block body %}

<h1>Här kommer info om sessionen att vara</h1>

<p>The session contains:</p>

{{ dump(session) }}

{# <ul>
    <h2>Kort i kortleken</h2>
    {% for cards in deckValues %}
        <li>{{ cards }}</li>
    {% endfor %}
</ul> #}

{% endblock %}

