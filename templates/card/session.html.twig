{% extends "base.html.twig" %}

{% block title %}Session{% endblock %}

{% block body %}

<h1>Info om sessionen</h1>

<p>The session contains:</p>

{{ dump(session) }}

{# <ul>
    <h2>Kort i kortleken</h2>
    {% for cards in deckValues %}
        <li>{{ cards }}</li>
    {% endfor %}
</ul> #}

{% endblock %}

